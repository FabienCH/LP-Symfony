{% extends "mi06VitrineBundle::layout.html.twig" %}
{% block content %}
    <div class="col-lg-12">
        <h1>{{ categorie.intitule }}</h1>
        <div class="row">
            {% for article in articles %}
            <div class="col-lg-4 col-md-6 mb-4">
                <div class="card h-100">
                    <a href="#"><img class="card-img-top" src="http://placehold.it/700x400" alt=""></a>
                    <div class="card-body">
                        <h4 class="card-title">
                            <a href="#">{{ article.libelle }}</a>
                        </h4>
                        <h5>{{ article.prix }} €</h5>
                        <p class="card-text">stock : {{ article.stock }}</p>
                        <p class="card-text"><a href="/ajoutArticle/{{ article.id }}/quantite/1">ajouter au panier</a></p>
                    </div>
                    <div class="card-footer">
                        <small class="text-muted">&#9733; &#9733; &#9733; &#9733; &#9734;</small>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
        <!-- /.row -->
    </div>
    <!-- /.col-lg-9 -->
{% endblock %}