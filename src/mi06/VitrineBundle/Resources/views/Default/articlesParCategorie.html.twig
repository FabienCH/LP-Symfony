{% extends "mi06VitrineBundle::layout.html.twig" %}
{% block content %}
    <div class="col-lg-12">
        <h1>{{ categorie.intitule }}</h1>
        <div class="row">
            {% for article in articles %}
            <div class="col-lg-4 col-md-6 mb-4">
                <div class="card h-100">
                    <a href="#"><img class="card-img-top" src="http://placehold.it/700x400" alt=""></a>
                    <div class="card-body">
                        <h4 class="card-title">
                            <a href="#">{{ article.libelle }}</a>
                        </h4>
                        <h5>{{ article.prix }} €</h5>
                        <p class="card-text">stock : {{ article.stock }}</p>
                    {% if article.stock %}
                        <p class="card-text"><a href="{{ path('mi06_ajout_article', {'idArticle': article.id, 'quantite': 1}) }}">{{ 'mi06.vitrine.addBasket'|trans }}</a></p>
                    {% endif %}                    
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
        <!-- /.row -->
    </div>
    <!-- /.col-lg-9 -->
{% endblock %}